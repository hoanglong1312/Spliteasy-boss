{
  "mcpServers": {
    "codex": {
      "type": "stdio",
      "command": "codex",
      "args": ["mcp-server"],
      "env": {
        "NINEROUTER_API_KEY": "${NINEROUTER_API_KEY}"
      }
    }
  }
}
